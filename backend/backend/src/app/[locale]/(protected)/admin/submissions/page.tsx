import { SubmissionsPageClient } from '@/components/admin/submissions-page';

/**
 * Admin submissions page
 * Display all tool submissions from all users (admin only)
 */
export default function AdminSubmissionsPage() {
  return <SubmissionsPageClient />;
}
