import { SubmissionsList } from '@/components/settings/submissions/submissions-list';

/**
 * User submissions page
 * Display all tool submissions by the current user
 */
export default function SubmissionsPage() {
  return <SubmissionsList />;
}
